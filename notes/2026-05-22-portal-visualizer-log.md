# Portal Visualizer Log

## 2026-05-22

- Created a backend-first local prototype instead of starting with Electron or a full SPA.
- Added an env-driven connector layer with `mock`, `http-json`, and `http-html` modes.
- Added an optional OpenAI summary layer that uses env-based configuration and disables storage on API calls.
- Added a minimal local browser page to exercise health checks, portal preview loading, and optional summaries.
- Added a smoke test script to validate the basic flow with mock data.
- Installed runtime dependencies: `express`, `dotenv`, `openai`, and `cheerio`.
- Verified locally with `npm run smoke`; the app served the page, returned a healthy API response, and loaded 3 mock records.
- Confirmed the real target page `https://tools.benchmarkdigital.com/gsportal/index_old.cfm` redirects to Microsoft login when not already authenticated.
- Found the stored Microsoft username env var appears to contain a typo, so automatic inspection of the live portal table is still blocked.
- Added HTML request-table crawling support that can follow row links into detail pages and attach detail-page content to each normalized record.
- Added explicit SSO redirect detection so unauthenticated fetches fail with a clear message instead of parsing the Microsoft login page.
- Switched live page inspection to Chrome DevTools and confirmed the active request list lives inside an iframe on `index_old.cfm`.
- Confirmed on May 22, 2026 that the selected Customer Requests tab loads `index.cfm?todolist...` and exposes row links like `?editid=` and `?itemhm=`.
- Added iframe-aware HTML extraction so the backend can fetch the outer portal page, resolve the embedded request-list iframe, parse request rows, and optionally follow linked detail pages.
- Extended the smoke test to validate the full outer-page -> iframe -> linked-detail flow.
- Added a new `browser-html` portal mode backed by Playwright and a persistent local browser profile.
- Added browser executable auto-detection for local Chrome or Chrome Beta installs and env settings for executable path, user-data dir, headless mode, and navigation timeout.
- Refactored HTML extraction into a shared pipeline so `http-html` and `browser-html` use the same iframe and linked-detail parsing logic.
- Verified on May 22, 2026 with `npm run smoke` that mock mode, raw HTML mode, iframe mode, and browser-backed mode all pass locally.
- Updated the live `.env` to use the normal Chrome Beta `User Data` profile instead of the app-specific `.local-browser/portal-profile`.
- Fixed the profile launcher to read `.env` before opening Chrome Beta so manual login tests use the same profile path as the app.
- Added a clearer browser launch error when the selected Chrome Beta profile is already in use by another Chrome process.
- Adjusted the browser-auth handoff to close stray `about:blank` tabs when Microsoft login is shown.
- Confirmed Chrome blocks Playwright DevTools against the default Chrome Beta profile and added an automatic mirrored-profile launch path under `.local-browser/chrome-beta-user-data-mirror`.
- Verified the live app now mirrors the normal Chrome Beta profile source, launches from the mirrored profile, and successfully loads the real Benchmark request iframe.
- Removed inline scripts and token-like secrets from extracted detail-page content and raw previews before returning snapshots to the browser UI.
- Added browser-session recovery so if the mirrored Chrome window is closed manually, the next preview request recreates the Playwright session instead of failing with `browserContext.newPage: Target page, context or browser has been closed`.
- Changed mirrored-profile sync to tolerate locked files from the normal Chrome Beta source profile, so an open default browser no longer fails preview requests on `Cookies` copy locks.
- Inspected the `gentle-day-ai-proxy` `newDevWclaudeAI` branch and reused its `testchat` pattern as the reference for a new local `POST /api/portal/parse` route.
- Added a portal parser service with structured JSON-schema mode and raw `testchat` bypass mode, plus a minimal parser control in the local browser UI.
- Switched the app default OpenAI model from `gpt-5` to `gpt-4.1-mini` in config, `.env`, and setup examples.
- Updated the app default OpenAI model from `gpt-4.1-mini` to `gpt-5.4-mini` in config, `.env`, and setup examples.
- Clarified the local UI labels so the summary path and parser path are easier to distinguish, and added explanatory helper text under both options.
- Added forced default control values on page load: summary enabled, parser test chat disabled, and prefilled prompts for both summary and structured parsing.
- Reworked the frontend from a debug-first test harness into an auto-loading dashboard with a readable action brief, top-line stats, and distinct to-do cards for each portal item.
- Added a consolidated `POST /api/dashboard` route so the page can fetch one portal snapshot and derive both the summary and structured parser output from the same data load.
- Restricted the OpenAI-bound portal payload to only six fields per request item: `Application`, `Business`, `Due Date`, `Request Details`, `Assigned Lead`, and `Request History`.
- Added detail-page field extraction so the app derives those six fields from full request pages before clipping or sending any model input.
- Removed the parser-side `Focus` field from the JSON payload so the model-bound data body now contains only the six allowed portal fields, with user focus carried separately as instructions.
- Added a smoke-test regression check that parses the exact OpenAI-bound payload and fails if any top-level or per-record keys fall outside the allowed schema.
- Tightened portal field-label matching to prefer exact-case labels first, which prevents the `Business` extractor from accidentally matching lowercase prose like `business activation` before the real field label.
- Rebuilt the frontend into a dark mission-control layout with a featured `Do This First` spotlight, calmer action brief, compact telemetry cards, and visually tucked-away `Advanced AI controls` and `Diagnostics` sections.
- Replaced the prior light serif dashboard styling with a tokenized electric dark UI system: glass panels, glow accents, clearer urgency states, responsive grid behavior, visible focus rings, loading shimmer, and reduced-motion support.
- Updated client-side rendering to preserve existing IDs and data flow while adding a top-priority spotlight, formatted summary markup, loading state handling, more scannable task cards, and progressive disclosure for longer task context.
- Added a data-URL favicon to eliminate the browser console 404 for `favicon.ico`.
- Switched the dashboard boot flow from auto-refresh to cache-first local rendering: the app now opens from the last saved dashboard JSON in browser storage and only fetches fresh portal data when `Refresh queue` is clicked.
- Added saved-snapshot freshness indicators with escalating urgency states (`Fresh`, `Aging`, `Stale`, `Old`) based on the age of the stored portal snapshot, plus a human-readable age note and board metadata update.
- Added an explicit no-cache empty state so first-time loads explain that a manual refresh is required before any portal data can be shown or saved.
- Added an Electron desktop shell that starts the existing embedded local server on a random localhost port and loads it into a native app window without changing backend routes or app behavior.
- Added Windows helper launchers for double-click usage: `Launch Portal Visualizer.cmd` for the local shell and `Build Portal Visualizer EXE.cmd` for packaging.
- Added Electron packaging scripts and `electron-builder` config so the project can produce Windows installer and portable executable outputs under `dist\`.
- Configured the desktop shell to use a writable Electron user-data working directory while loading `.env` from the project root in development or next to the packaged `.exe` when distributed.
- Changed the double-click launcher to prefer the unpacked built shell (`dist\win-unpacked\Portal Visualizer.exe`) and build it on demand, which avoids depending on the local Electron dev runtime for normal desktop use.
- Switched the project to Electron-first startup: `npm start` now launches the desktop shell, browser-only server scripts were renamed to explicit dev/debug commands, and README guidance now treats browser tabs as a fallback rather than the normal workflow.
- Changed the desktop launcher to prefer the local Electron runtime from `node_modules` before falling back to a built unpacked shell, which keeps the working folder portable without requiring an installer.
- Locked the embedded app server to `127.0.0.1` for shell traffic and smoke tests so the local dashboard stays loopback-only instead of behaving like a broader localhost web entry point.
- Refined the shell launcher again after local Electron runtime gaps were discovered on this machine: the primary path now rebuilds and launches the unpacked portable shell when source files change, and the Windows handoff uses `Start-Process` instead of relying on a browser-style workflow.
- Updated the packaged Electron shell env lookup so an unpacked build launched from `dist\win-unpacked` can still find the project-root `.env`, which restores OpenAI-backed summary behavior during normal local shell use.
- Removed visible AI/OpenAI wording from the main dashboard summary area and advanced controls, and replaced the summary focus chip with a full-width focus card so the main UI reads like a work dashboard rather than a tooling panel.
- Added launcher-side `.env` syncing for the unpacked shell so local packaged runs reliably inherit the same OpenAI and portal settings as the source project without needing manual file copies.
- Updated detail-page extraction so `owner` / `Assigned Lead` now prefer the live `#DevItem` field value from the request page instead of the row-level owner text, and added smoke-test coverage for that override in HTML, iframe, and browser-backed modes.
- Removed the visible Parser Mode stat card from the dashboard while keeping the internal parser-mode hook intact for rendering logic, and added a reusable sheen-hover treatment to the visible dashboard cards so Open Items, Do Now, Blocked, Action Brief, Do This First, and to-do cards all get the same diagonal glint/lift effect without changing their base look.
- Switched the default `npm run dev` workflow back to browser debugging by adding a watch-mode launcher that opens the local app URL, and moved the Electron rebuild flow to `npm run dev:desktop`.
- Improved browser-debug diagnostics so failed refreshes keep any successful `/api/health` response visible and replace the generic `Failed to fetch` message with guidance to open the app from the local server URL instead of a file or unrelated static origin.
- Added a repo-root `handout.md` with a Claude-ready transfer summary covering the current dev commands, browser-origin debugging caveat, verified API behavior, and the key files to inspect next.
- Added a shared portal cookie resume cache backed by a process-global store plus `.local-auth/portal-cookie-cache.json`, and taught `browser-html` to seed it so later `cookie-html` runs can resume without reopening the browser.
