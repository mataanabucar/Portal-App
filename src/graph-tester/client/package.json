{
  "name": "graph-tester-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Vue 3 + Vite UI for the standalone Microsoft Graph Tester. UI only — Express owns auth, tokens, Graph calls, the catalog, and validation.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "type-check": "vue-tsc --noEmit"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@vitejs/plugin-vue": "6.0.7",
    "typescript": "^5.7.2",
    "vite": "8.1.4",
    "vue-tsc": "3.3.7"
  }
}
